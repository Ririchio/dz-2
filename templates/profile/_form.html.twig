<div>
        {{ form_start(form) }}
            <div style="margin: 5px;">
                {{ form_label(form.bio) }} 
                {{ form_widget(form.bio) }}
            </div>
            <div style="margin: 5px;">
                <button>Сохранить!</button>
            </div>
        {{ form_end(form) }}
</div>
