{% extends 'base.html.twig' %}

{% block title %}LuckyNumber{% endblock %}

{% block body %}
    <h1>LuckyNumber</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lucky_number.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ lucky_number.value }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ lucky_number.author }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lucky_number_index') }}">back to list</a>

    <a href="{{ path('app_lucky_number_edit', {'id': lucky_number.id}) }}">edit</a>

    {{ include('lucky_number/_delete_form.html.twig') }}
{% endblock %}
